"use strict";
/*

/import { fetchPosts } from 'api';
/import { User } from 'types';

fetchPosts()
    .then((posts) => {
        console.log(posts);
    })
    .catch((error) => {
        console.error(error);
    });

let user: User = { name: 'Alice', age: 30 };
console.log(user);

*/ 
