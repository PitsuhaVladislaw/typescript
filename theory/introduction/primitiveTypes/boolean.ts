let isStudent: boolean = true;
let isAdult: boolean = false;

console.log(isStudent);
console.log(isAdult);

let isWorker: boolean = true;
let notHasJob: boolean = false;

if(isWorker) {
    console.log("The person is a worker.");
} else {
    console.log("The person isn't a worker");
}