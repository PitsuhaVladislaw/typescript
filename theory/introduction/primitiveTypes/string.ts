let nameP: string = "John";
console.log(nameP);

let message: string = `Hey, ${nameP}`;
console.log(message);


let firstName: string = "Jack";
let secondName: string = "Sparrow";

let fullName: string = `${firstName} ${secondName}`;
console.log(fullName);